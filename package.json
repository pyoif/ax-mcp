{
  "name": "ax-mcp",
  "version": "1.0.0",
  "description": "MCP server for ax — the AI-era curl",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ax-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "prepare": "pnpm build",
    "prepublishOnly": "pnpm build"
  },
  "keywords": [
    "mcp",
    "ax",
    "fetch",
    "scrape",
    "css-selectors"
  ],
  "license": "GPL-3.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "peerDependencies": {
    "ax": "*"
  }
}
